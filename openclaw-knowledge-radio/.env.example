# Copy this file to .env and fill in your values.
# Then load it before running: export $(cat .env | xargs)
# Or use: source .env

# ── Required ───────────────────────────────────────────────────────────────────
# OpenRouter API key — free account at https://openrouter.ai
# After signing up, go to: https://openrouter.ai/settings/keys → Create key
# The free-tier models in config.yaml cost nothing.
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# GitHub personal access token — needed to upload audio to GitHub Releases
# and push the updated GitHub Pages site after each run.
# Create at: https://github.com/settings/tokens → Generate new token (classic)
# Required scope: repo
GITHUB_TOKEN=ghp_your-token-here

# ── Optional: Notion archive ───────────────────────────────────────────────────
# Saves each day's full script + item list as a Notion database page.
# Setup steps are in src/outputs/notion_publish.py (top of file).
# NOTION_TOKEN=secret_xxxx
# NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Optional: Slack notifications ─────────────────────────────────────────────
# Paste your Incoming Webhook URL here to receive a daily digest in Slack.
# How to get one:
#   1. Go to https://api.slack.com/apps → Create New App → From scratch
#   2. Features → Incoming Webhooks → Activate → Add New Webhook to Workspace
#   3. Choose a channel (e.g. #knowledge-radio) → Allow → copy the URL below
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../xxx

# ── Optional overrides ─────────────────────────────────────────────────────────
# Override run date (YYYY-MM-DD). Leave unset to use today.
# RUN_DATE=2026-02-28

# How many hours back to look for new articles (default: 48)
# LOOKBACK_HOURS=48

# Set to "true" to skip real LLM calls and use cached/stub data
# DEBUG=false

# Rebuild podcast script from already-fetched items (skips RSS/PubMed fetch)
# REGEN_FROM_CACHE=false

# Tip: to reduce parallel LLM calls on the free tier, set fetch_workers: 4
# in config.yaml under the top-level keys.
